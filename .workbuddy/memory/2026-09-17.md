# 2026-09-17

## 路线 B：批量翻译长程滑窗上下文 + 受控并发池（三线全绿）

- **滑窗上下文**（`core/pipeline/translator.py`）：
  - `TranslationConfig` 新增 `context_lines: int = 4`（0=关闭）；User Prompt 顶部注入
    `【前文背景参考（仅供理解语境，无需翻译）】` + 行为约束行 + `【角色】译文` 行块；
    **上下文只进 User 角色**，System Prompt 保持身份+术语（编号解析面不被污染）。
  - 「已定稿」口径：非空译文即算（与防丢失判定同倾向）；上下文在**发包计划期一次性快照**
    —— 并发乱序完成不可能反写后续子批 Prompt，同输入恒同载荷。
  - 来源两级：RPC `translate_batch` 新增可选 `context` 参数（壳层传已定稿行）；缺省回退
    批内先行定稿单元（直调管线场景）。无前文时逐字零冗余。
  - 畸形 context 项（非 dict/缺 text/空白）静默跳过，不整批失败。
- **受控并发池**：`concurrency_limit: int = 3`；ThreadPoolExecutor（_post 是同步 urllib，
  零新依赖）；回填按提交序（=全局索引序）收敛，译文与物理行绝对对齐；任一子批失败
  整批 TranslationAPIError 判死（与串行语义一致）。429 退避 sleep 在 worker 线程内，
  单任务等待不阻塞健康任务（已有 fake_sleep=Event 栅栏的确定性测试钉死）。
- **C# 接线**：`TranslationContextLine(Speaker, Text)` DTO + `TranslateBatchAsync` 可选
  `context` 参数；`StudioViewModel.BuildBatchContext()` 按文档序取最近 4 行有译文行
  作实参逐片求值（先完成切片天然成为后续切片前文）；`RetryUnitAsync` 滑窗排除被重试
  行自身（旧坏译文不得反向锚定模型）。
- **关键解禁**：壳层原本 `config with { BatchSize = payload.Count }` 把 Sidecar 钳成
  单子批——并发池在 UI 链路永不生效的软花架子。已删除钳制，`TranslateChunkSize`
  8 → 24（= batch_size 8 × concurrency 3），切片只承载进度分档。契约测试钉死
  （`BatchSize = payload.Count` 回归即红）。
- **测试**：+10（滑窗 6：隔离块注入/零冗余/批内前文/窗口截断/0 关闭/快照不泄漏；
  并发 3：乱序保序、并发度精确=2（真实 sleep 0.05 观测）、429 不阻塞健康任务
  （fake_sleep 内 assert b_done.wait(5)——串行退化必红）；契约 30→31）。
  **变异验证**：workers 强制=1 → 3 条并发测试全红，还原后逐字节一致。
- **环境坑（重要）**：宿主 Shell 会注入 `HTTP_PROXY=http://127.0.0.1:<port>`（CLI shim
  服务代理），urllib 把发往必然关闭端口的请求经代理送出、被代理答 502——
  「连接拒绝」类网络用例前提失效。修法：测试内 `monkeypatch.delenv` 代理变量 +
  `setattr(getproxies, lambda: {})` + `install_opener(None)`（并 finally 还原 opener，
  全局缓存 opener 不会自动重建）。
- **工具坑**：同一文件的**并行双 Edit 会互相覆盖（后写者赢）且都报成功**——本轮
  PythonSidecarClient.cs 与 StudioViewModel.cs 各被吞掉一处改动，编译期 CS0103/CS1503
  才暴露。同文件多处修改必须串行执行，写后 grep 复核。
- **三线**：`dotnet build --no-incremental` 0 警告 0 错误；`dotnet test` **114/114**；
  `pytest tests/` **328**（318→328）。实例未运行；未 git add/commit/push。

## P1 收尾：Token 真实计量 + LQA Lab 真实面板化（三线全绿）

- **Token 链路**：translator 内互斥锁累加 `_usage`（计入点=成功收包，429 重试不重复计费；
  缺失/负数/非数值按 0 钳制——「计量可以缺失，不可以是错的」）；每次 translate_batch 清零重计；
  **RPC 契约升级**：响应从 `[units]` → `{"units": [...], "usage": {...}}`；C# 侧
  `TokenUsageDto` + `TranslateBatchResult` 信封；壳层 `SessionTotalTokens` 累加
  （批量+单条重试两处），底栏 `Session: N Tokens`（无消耗空串不显示）。
- **LQA Lab 真实化**：`GalPipeline.Core/Lqa/LqaBoard.cs` 纯函数聚合（rule_id 分组、
  error 优先排序、同单元多违例合并一条、空输入短路 `LqaBoardSnapshot.Empty`——
  物化后判空，惰性枚举直判 ICollection 会漏算）；`StudioViewModelProvider.Shared`
  进程级单例（跨导航保留状态，LqaLab 才能聚合/定位/重译工坊实时行）；
  `FocusUnit(id)`（清筛选再选中）+ `FocusRequested` 静态事件（MainWindow 导航回 Studio）；
  LqaLabPage 重写为看板：统计 + 规则徽章 + 违规列表（行号/级别/规则/描述/原译对比）
  + 定位/定向重译按钮 + 全绿态/未载入态。StudioView Unloaded 退订单例事件防泄漏。
- **连带破坏教训**：改共享客户端契约（返回类型 List→信封）击穿了 `src/GalPipeline.UI`
  WinUI 遗留工程（也在消费 PythonSidecarClient！CS0411 类型推断失败 +
  XamlCompiler WMC9999 次生噪音混淆视听）——改共享面必须 grep 全解决方案调用面。
- **测试**：+7（TokenUsage 5 + 契约 31→33：LqaLab 真实化 / Session 真实计量）；
  C# +6（LqaBoardTests）。**三线**：0/0 · **120/120** · **335**。未 git add/commit/push。


## P2：Asset Hub 真实看板化 + TLG 私有图像互转管道（三线全绿）

- **TLG5 内置解码器**（core/media/tlg5.py，纯 Python 零依赖）：规格经
  **双源交叉验证**——krkrz-mingw/krglhtlg LoadTLG.cpp（头 colors/w/h/**blockheight**、
  索引表跳过不校验、mark=0→Slide LZSS/非0→raw、BGR(A) 平面、b+=g/r+=g 预测、
  行内累加）+ tlg-rs crate（Slide LZSS 权威：**flag 位 1=match/0=literal**、
  pos 12bit、len nibble 0xF→18+1 字节、字典 4096+镜像 272、状态跨块跨通道持续；
  平面语义 = **R-G/G/B-G/A mod-256 差分**，prevcl 是未取模 int 链）。
  **TLG6 不内置解码**（凭印象实现=假交付），走外部工具链 CLI 通道
  （命令模板登记 `_CONVERT_RECIPES`，工具路径由壳层解析传入，未装配→装配引导）。
- **RPC convert_image(src_path, dst_path, tool_path=None)**：TLG5 内置 →
  Pillow 落 PNG；其余变体外部通道；异常分型 MediaConversionError /
  UnsupportedImageVariantError（均继承 EngineAdapterError → -32000）。
- **Asset Hub 真实化**：`GalPipeline.Core/Project/AssetWorkspace.cs` 纯函数
  （三分类扫描 scripts/images/voice+bgm→audio、**目录+扩展名双重校验**
  ——voice/ 里的 txt 不算脚本、单类 2000 截断、TLG 缓存指纹 SHA256(路径|size|mtime)）；
  AssetHubPage 占位废除 → 三标签看板 + 统计徽章 + 图像即插即看预览
  （TLG 自动转缓存 PNG 显示）+ 双击（脚本→工坊载入+导航 / TLG→转换后打开 / 其余→系统查看器）
  + 打开资产目录。`StudioViewModelProvider` 增 `NavigateRequested` 通用导航事件。
- **调试血泪（两条都记档）**：① Slide LZSS 位语义（1=match）凭直觉写反，
  解码器/合成编码器**同错互洽**→ 必须以权威源码（tlg-rs）为锚，双错互检是假绿；
  ② 合成差分链 prevcl 必须是**未取模 int**，只有写平面时才 mod 256。
- **测试**：+21（Python media 15：Slide 位序/match/扩展/跨块状态（**切分必须在
  flag 组边界**——块边界=组边界）、像素级 roundtrip 4 组、SDS、TLG6 引导、
  外部垫片、RPC 帧；C# AssetWorkspace 6）；**三线**：0/0 · **126/126** · **350**。
  xUnit2013 警告（Assert.Equal 集合数→Assert.Single）也清零。未 git add/commit/push。


## 工具链闭环：Settings 页工具链环境面板（三线全绿）

- **快照扩展**：`TranslationSettingsSnapshot.CustomToolPaths`（null=未指定）+
  `WithCustomToolPath` 不可变合并 + `ToolPathsEqual` 按内容比较（字典默认引用相等，
  快照合并语义必须按内容）。仓库新增 `SetCustomToolPath`（空白=清除条目、
  **内容防抖后才发 ToolchainChanged**）/ `GetCustomToolPath`。
- **`ToolchainRuntime`**（Core/Toolchain）：订阅 ToolchainChanged → 解析器整体重建 +
  `ResolverChanged` 失效通知。消费方纪律：**严禁自建 ExternalToolResolver**
  （契约测试钉死 StudioViewModel 与 FFmpegAudioCache 两处）；FFmpegAudioCache
  订阅失效通知清 `_resolvedFfmpegPath`，Dispose 退订防静态事件钉实例。
- **PushToStore 必须携带 CustomToolPaths**：快照整体替换语义，不带=改一次 API
  URL 就静默清空工具路径。这是本轮自己设计出的最大坑，已契约化。
- Settings 页卡片：`ToolchainToolViewModel` 行级命令（Browse/Reset/OpenDirectory）+
  状态徽章（Ready 绿 / Missing 灰 / 其余红）+ 版本 + 来源标签 + 生效路径；
  `ToolBox24` 图标不存在于 Wpf.Ui 4.3 → 换 `Wrench24`（已二进制检索确认）。
- **测试**：C# +13（快照合并/持久化/清除/防抖/运行时重建/Custom 层命中；
  进程级仓库跨类并行会互踩 → 新增 `ProcessLevelStoreCollection` 集合定义双类串行）；
  契约 33→34。**三线**：0/0 · **139/139** · **351**。未 git add/commit/push。
- **重复踩坑第 3 次**：`assert "X" not in source` 被警示注释命中（注释里写着
  反例本身）—— code_only() 剥注释模式已再次落地，该断言写法必须肌肉记忆化。


## 通用中继适配器（core/adapters/relay/，三线全绿）

- **架构转向（Agent 0 裁决）**：本地化工作站严禁再手写私有引擎解析器；
  引擎接入 = 社区 CLI dump → 中间文本 → 通用适配器 → Gal-IR → 社区 CLI 回封。
  三适配器（tabular/marked/json_relay）继承 BaseEngineAdapter，注册
  relay.tsv/relay.csv/relay.marked/relay.json，零新 RPC、零 C# 改动。
- **配方与能力正交**：`recipes.py` 的 RelayRecipe 声明引擎语义
  （krkr_psb→relay.json+patch2.xp3 / cs2→relay.marked+update.int /
  bgi→relay.tsv+LooseDirectory / generic_garbro 只读兜底）；
  supports_recompile=False → assert_can_recompile 抛 RelayCapabilityError
  （继承 EngineAdapterError，回封闸门；中间文件仍可产出）。
- **保真实现要点**：newline='' 禁万能换行翻译；行终止符探测复用；
  BOM 对称保留（utf-8-sig 恒加 BOM 是坑——检测后选择写法）；
  JSON **行内手术替换**（行导向缩进 JSON，白名单文本键，其余节点天然锁定；
  严禁 loads/dumps 全文往返——会重排缩进与转义）；marked metadata 冻结
  target_line_raw，未译/译文未变整行原样保留。
- **测试**：23 条（抽取/Pristine 逐字节/译文落位/幂等/CSV 引号保真/
  能力阻断/注册表/RPC 帧/LQA 共享门禁）+ **变异验证**（tabular 回写
  无中生有 → pristine 红 → 还原逐字节一致）。
- **踩坑**：①csv roundtrip 需 writer/reader 同方言（TSV 测试误用 Tab 写
  CSV 读）；②keepends 行的终止符会混入正则捕获组（DOTALL）——匹配前
  rstrip("\r\n")；③pytest 旧临时目录批量清理触发安全删除闸（321 文件>
  阈值）——用 --basetemp 指独立目录旁路；④detect 结构置信需 ≥2 行。
- **三线**：0/0 · **139/139** · **374**（351→374）。未 git add/commit/push。


## EngineToolchainBridge 调度器（core/engine/bridge.py，三线全绿）

- 双向中继调度：dump_to_intermediate / compile_from_intermediate；
  模板在 RelayRecipe 上（krkr_psb/cs2 已登记草案：`{tool} -d {input} -o {output_dir}` 等），
  **占位符必须独占 token**（argv 列表承载空格路径）；bgi 未校准 → 受控拒绝。
- 治理：60s 超时 → taskkill /F /T 树杀（异常携带 pid 供测试核实死亡）；
  **输出用临时文件而非 PIPE**——树杀后管道读取线程竞态 = pytest 线程警告来源；
  产物识别用 **mtime ≥ 启动时刻窗口**（同名覆盖的新产物必须胜出，路径差分会误拒——
  首版用快照差分是设计错误，测试当场纠正）；非零退出截 stderr 尾 400 字符。
- **用户命令暴露真缺陷**：`--basetemp=.pytest_temp`（仓库根含空格）让
  media 外部模板 `{tool}` 无引号的问题现形 → 全占位符加引号并记档。
  通用教训：**测试基建参数变化会暴露侥幸存活的潜在 bug，全绿≠无病**。
- 垫片坑：json.dumps 生成 false/true 不是 Python 字面量；tasklist 中文输出
  GBK 与 utf-8 text 模式冲突 → errors="replace"。测试 +12（bridge 12）。
- **三线**：0/0 · **139/139** · **386 零警告**。未 git add/commit/push。


## krkr_psb 端到端闭环 + FreeMote 模板校准（三线全绿）

- **配方校准**：krkr_psb 模板按 FreeMote 官方 CLI 规范登记
  （PsbDecompile `-o {output_dir} {input}` / PsbBuild `{input} -o {output}`，
  dump_tool/recompile_tool 更新为 PsbDecompile.exe / PsbBuild.exe）；
  新增 `templates_calibrated` 一等元数据（krkr_psb=True，cs2/bgi/garbro 如实 False）——
  「校准与否」从口头约定变成可断言事实。argv 列表规范：占位符严禁加引号。
- **E2E 替身策略**：本机无 FreeMote（离线红线），测试用高保真 PSB 容器替身
  （magic "PSB\0" + u32le 长度 + JSON 载荷；build 前验 JSON 合法性）——
  替身验证的是**我方全链路**（bridge→relay.json→手术回写→bridge），
  真实 FreeMote 装配后换工具路径即自动接管。
- **E2E 断言**：载荷仅 text 值一行变化（缩进/换行/非文本节点 ver/res.hash/
  motion 逐字节不动）；pristine 全链路载荷逐字节一致；产物名 `scene.psb.json`
  （FreeMote 命名约定）；OverlayPatch 暂存按**原相对路径**（game/scenario/…）
  收纳进 patch 根。
- **三线**：0/0 · **139/139** · **392 零警告**（386→392：E2E 6 条）。
  未 git add/commit/push。


## 打包分发引擎（core/packaging/，模块七，三线全绿）

- **Dirty-Only 两级判定**：size+mtime 双一致免哈希；否则 SHA-256 比对。
  mtime 变化但内容未变 → 干净（专测钉死）；removed 仅 manifest 披露不进载荷。
  AssetDiff 自带 source（坏味道教训：模块级注册表在收集/打包间传状态已否决）。
- **三策略路由**：OverlayPatch（暂存根 {artifact}.d + pack_command 打包，
  无工具受控降级为暂存+指引）；LooseDirectory（解压即用覆盖）；
  XDeltaDiff（xdelta3 -e 生成 + **-d 还原验证哈希一致**，恶意替身还原不一致
  → 拒绝交付）。交付根 {game_id}_patch 幂等重建 + manifest.json + INSTALL.txt。
- bridge：提炼公共 `run_tool(command, verb)`（打包/差分复用进程治理）；
  `_execute` 消息改 label 参数（重构漏改超时消息 → NameError，Edit 后逐处 grep 复核）。
- **环境事故**：pydantic-core 被外部改为 2.49.0（pydantic 2.13.5 要求 2.46.5）
  → 全仓 import 崩溃；`pip install pydantic-core==2.46.5` 精确回钉修复。
- **三线**：0/0 · **139/139** · **404 零警告**（392→404：packaging 12）。
  未 git add/commit/push。


## 工具链审计 + 真实装配 + 真机 FreeMote E2E（三线全绿）

- **用户裁决**：离线红线 = 资产数据不外发 + GPL 零链接，**不禁联网取工具**——
  此前执行侧误读已纠正。
- **audit 脚本**：配方 ∪ 注册表静态聚合 → tools/rglob + PATH 扫描 →
  Found（版本实测）/ Missing（来源+命令+直链）。正则踩坑：C# verbatim
  字符串 `@"..."` 要 `@?"` 才匹配。
- **download 脚本**：GitHub API 现查 latest（不硬编码版本）；SHA256SUMS 留痕；
  zip EOCD 完整性校验（SIGTERM 截断包曾冒充成功——9.4MB/90MB）；GUI 工具
  跳过启动探测。代理对大文件/rar 不稳：ffmpeg 与 GARbro 未落地（可重跑续拉）。
- **实测推翻凭印象登记（拒绝猜测的回报）**：FreeMote v4.7.0 真实 exe =
  PsbDecompile.exe + **PsBuild.exe**（无 FreeMote.Psb.exe/PsbBuild.exe）；
  仓库是 UlyssesWu（uesugichun 404）；两者 `-o` 参数位与已登记模板吻合；
  PsbDecompile 同产 resx.json → bridge `_newest_product` 补 expected_stem 消歧。
  注册表已修（URL/SHA256/ExecutableName 实名）。
- **真机 E2E（gated）**：真 PsbBuild→psb→真 PsbDecompile→relay 译文→真
  PsBuild 回编译→真机复核译文进二进制。生成链 `\x00` 双层转义吃掉一层
  → 源文件真 NUL 字节 → 二进制修补。
- **三线**：0/0 · **139/139** · **406 零警告**（404→406）。tools/ 已 gitignore。
  未 git add/commit/push。

## 用户手动补货工具箱（14:15-14:46 落盘 9 个压缩包）

- 直接命中审计缺口：BGI_Hazuki v1.1.4（BGI-Tools：TextTool/ImageTool/AssetTool/Unpacker）；
  CatSystem2-Simple-Translating-Tools（cs2-tools：cs2_decompile/exkifint_v3/exzt/hgx2bmp/mc + py 脚本，
  依赖 pandas/xlsxwriter/openpyxl/colorama）；xdelta3 3.2.0（升级版）。
- 通用工具：VNTranslationTools（VNTextPatch.exe，自带 FreeMote.Psb.dll，多引擎文本抽取/回填）。
- 仅源码需构建：GARbro-Mod-master（1356 个 .cs，无 exe）；HookFont-Test-main（C++ sln，字体替换注入）。
- 运行时辅助非 CLI：Locale.Emulator 2.5.0.1（LEProc/LEGUI）；v1.5.zip = KrkrDump
  （KrkrDumpLoader.exe + KrkrDump.dll，krkr 运行时脚本倾倒钩子）。
- 综合工具集：chinesize-master（65 exe + 824 源码文件，krkr exkifint/exzt/hgx2bmp/arcpack/windmill 等）。
- 不可用：ffmpeg-9.0.1.tar.xz 是源码包，非 Windows 二进制，无法满足 ffmpeg.exe 缺口。
- 重复：Ulysses-FreeMoteToolkit-v4.7.0.zip 与已解压的 tools/freemote/ 同版。
- 尚未解压落位/改注册表——用户只要求先分类。

## 批量落位 6 步完成（15:08-15:30）

- 删 3 个错误包（2×arm64 + 1×网页假 zip）；FreeMote/HookFont 归档 _archive/。
- 落位：ffmpeg(N-126593 win64-gpl, ffmpeg+ffprobe 实测过)、bgi(BGI_Hazuki)、
  cs2(CatSystem2-STT)、vntextpatch(VNTextPatch)、xdelta→3.2.0、locale_emulator、
  krkr_dump(KrkrDump)。
- GARbro 源码编译成功（GARbro.Console.exe 实测可用）。四步排障：①缺 net472
  targeting pack → NuGet 拉引用程序集 + FrameworkPathOverride；②Newtonsoft/
  NAudio 等 35 个 HintPath 包 → 按 packages\ 约定从 nuget.org 归位（精确 404
  就近降级，33/35 成功，剩 2 个是条件 .targets 可跳过）；③CS0012 netstandard
  facade → GameRes.csproj 注入 HintPath=$(FrameworkPathOverride)\Facades\
  netstandard.dll；④MSB4063/4064 AL 任务 + MSB3030 ja-JP 卫星资源 → 摘除 5 个
  文化 resx（留中性 garStrings.resx）。供 Console 用足够。
- 实测全过：cs2_decompile(-i/-o)、exkifint_v3(需 input.int+game.exe)、
  BGI_Hazuki_TextTool/BGI_Unpacker(拖拽式)、VNTextPatch(extractlocal/insertlocal)。
- 审计终态：4 就绪 / 3 缺失。缺失全是命名缺口而非物理缺口：BGI-Tools、cs2-tools
  是抽象占位名（物理 exe 已在 tools/bgi、tools/cs2）；GARbro.exe 实为
  GARbro.Console.exe。待注册表/配方实名校准。
- chinesize 65 exe 隔离在 tools/_scan/chinesize/，按仓库目录归 19 组引擎
  （CatSystem2 8 个与 cs2 包重叠、Circus 11、N2System 6、System4 7、Majiro 5、
  Liar 5、ExHIBIT 3、ISM 3、Overflow 3、Malie 3、AdvTry 2、qlie 2、其余各 1），
  全部 x86，等人工挑选。

## 实名校准 → 工具链 8/8 全绿（15:45-16:05）

- 来源实证：BGI = ArisuMika520/BGI-Hazuki（v1.1.4 release 资产名逐字匹配）；
  cs2 包 = Wolverator/CatSystem2-Simple-Translating-Tools；GARbro-Mod =
  nanami5270/GARbro-Mod（morkt fork）。
- ToolType 枚举 +BGI +CatSystem2（消费点仅按值取用，无穷举 switch，扩容安全）。
- 注册表 4→6：GARbro.exe→GARbro.Console.exe（-l 探测，GameData/Formats.dat 同级，
  已实测）；新增 BGI_Hazuki_TextTool / cs2_decompile 条目；FFmpeg 下载通道切
  BtbN（gyan 实测被代理限速）。
- BGI TextTool 参数从二进制内嵌用法字符串实证（零猜测）：
  extract <script> [out.hazuki.txt] [--decode-cp 932] / apply <txt> [out] [--encode-cp 932]。
  cs2_decompile 实测 -i/-o/-u|-hr|-md；mc.exe=官方 Message Compiler（编译方向
  待真实样本 E2E）。bgi 配方双向绑 TextTool，cs2 绑 cs2_decompile+mc。
- GARbro.Console 缺 GameData/Formats.dat（从 ArcFormats/Resources 复制后 -l 正常）。
- chinesize 提取：majiro 5 exe / qlie 2 exe / circus 4 exe（comp/scecomp/ext(sce_ext)/
  DCPC图片编码）→ tools/{engine}/；_scan 整体入 _archive。
- 测试两处旧断言校准：ToolchainTests 条目数 4→6；bridge cs2 模板测试改 -i 风格。
- 终态：审计 8 就绪/0 缺失；dotnet 0/0 + 139/139；pytest 406/406 零警告。未 commit。

## Media 视觉修图工作台 Fluent 化（16:06-16:45）

- **纠偏（先认账）**：WPF-UI 4.3.0 其实早已在 csproj 里，App.xaml 已注入
  ThemesDictionary(Dark)+ControlsDictionary，MainWindow 已是 ui:FluentWindow +
  NavigationView + SymbolIcon。真正"毛坯"的只有 MediaPage（ui:InfoBar 占位壳）。
  故本轮实际交付：Media 工作台从零重建 + 主窗补 Mica + 全局转换器 + 契约测试。
- `dotnet add package Wpf.Ui` 会解析到 legacy 包 id（WPF.UI 3.4.2.7，net461 资产
  → NU1701 警告）—— 必须用 `dotnet add package WPF-UI`；已剔除误加引用，
  经 feed 确认 4.3.0 即最新稳定版。
- 新增文件：Services/LocalImageLoader.cs（缓存键=路径+mtime+size，OnLoad+
  IgnoreImageCache 强制穿透覆盖保存）、ViewModels/MediaViewModel.cs（16 个
  partial 属性 + 命令 + FileSystemWatcher 热重载 + NoticeRequested 事件）、
  ViewModels/MediaAssetItemViewModel.cs、Converters/MediaConverters.cs、
  Pages/MediaPage.xaml(重写)/.xaml.cs、tests/test_media_workbench_contract.py(14 测试)。
- **约定坑**：本仓 CommunityToolkit 用 `[ObservableProperty] public partial X { get; set; }`
  属性风格（不是 private _x 字段）——字段风格对静态绑定契约测试不可见，
  被测出 10 处"悬空绑定"后全部改为 partial 属性。
- XAML 硬坑：①XML 注释禁含 `--`（装饰线 `----` 直接 MC3000）；②ui:TextBox
  ShowClearButton 只读 → 用 ClearButtonAvailable? 实为 ClearButtonEnabled；
  ③StackPanel 无 BorderBrush → CardAction 行包 Border；④ui:DropDownButton.Flyout
  必须给 ContextMenu（源码实证）。
- 工具：本地源码 tools/wpfui-4.3.0 取代联网查 API（快且稳）。
- 三线：dotnet 0/0 · 139/139 · pytest 420（406→420）零警告。未 commit。

## Milestone E2E 真实全链路穿透（16:45-19:10）→ 三线 436 全绿

- 样本方案（诚实边界）：仓内无商业资产 → **准真实样本**：真 PsBuild 编译真 PSB
  (720B) + 真 PNG + 真 TLG5（tlg-rs 语义合成，自研解码器闭环）。全部解包/回编/差分
  由真机二进制执行，零替身。
- 新增 `tests/e2e/test_real_pipeline_penetration.py`（673 行 / 16 用例，五阶段 + 整链路）；
  新增 `core/launch/launch_config.py`（LaunchConfig：LEProc -run/-runas 契约实证 +
  启动器渲染 + 降级诊断）。阶段报告落 `.pytest_temp/e2e_penetration_report.json`
  （耗时+哈希），归档 `docs/reports/`。
- **D1 真产品缺陷**：`PackagingPipeline._build_xdelta` 还原验证参数位反向
  （`-d -s <补丁> <原版>` 应为 `-d -s <原版> <补丁>`）→ 真机 XD3_INVALID_INPUT；
  单测替身把同一错误序固化 → 静默全绿。已修 pipeline + 替身语义对齐真实契约。
- **D2 资产边界漏洞**：dump/回写中间件（scene.json / scene.psb.json）落进资产树被
  Dirty-Only 判为 added（3→5 项）进补丁。修：工作区纪律（work/）+ 交付守卫断言
  （暂存树禁 .json）+ 暗礁固化用例。产品侧建议（待裁决）：collector 对中间产物发 warning。
- **D3 认知修正**：relay.json 管线不登记宏 → 携带宏是 advisory(warning) 非 error；
  KAG 才是"剥离式+宏登记表"。relay 守恒由真机回读「宏恰好一次」保证。
- **D4**：替身模仿"实现"而非"外部契约" → 真机 E2E 作为防漂移基线。
- 三线：dotnet 0/0 · 139/139 · pytest **436**（420→436）零警告。未 commit。

## 收口固化：D2 机拦防御 + tools 规整 + 文档登记（19:43-20:05）

- **D2 机拦落地 `core/packaging/collector.py`**：`INTERMEDIATE_PATTERNS` 样式表
  （*.psb.json / *.resx.json / *.hazuki.txt / *.dump / *.tmp / *.bak / *~ / 兜底 *.json）；
  **最小误伤判据**：只拦「原版树中不存在」的**新增**文件（原版里的 .json = 引擎原生资产，
  绝不剔除）；`keep_globs` 白名单可放行原生新增 json。命中 → WARNING 日志 + 剔除载荷 +
  manifest `excluded_intermediates` 留痕（含命中样式与原因）。
  新增 API：`collect_dirty_report()`（结构化）+ `collect_dirty_assets()`（兼容，已剔净）。
  pipeline 改用报告式收集，`PackagingResult.excluded_intermediates` 暴露。
- 新护栏 `tests/test_intermediate_guard.py`（17 条，含 caplog 断言、白名单、原生资产
  不误杀、管道留痕、仅中间件的极端树受控交付）。E2E 暗礁用例升级为「机拦+留痕」语义。
- **tools/ 规整**：根目录**零散文件**（9 个源包 + wpfui-4.3.0 源码）全部入 `_archive/`；
  freemote 内重复源包按 sha256 去重删除；xdelta 旧版 3.1.0（binary+zip）入
  `_archive/xdelta3-3.1.0-superseded/`。终态：12 个生产工具目录 + `_archive/`，
  审计复验仍 **8 就绪 / 0 缺失**。
- **docs/AI_HANDOFF.md** 新增：§2.5 端到端架构拓扑（五段链路 ASCII 图）+ 8 CLI 在线状态表
  + tools/ 目录纪律；打包引擎章节补 D1/D2 收口；新增「Milestone E2E 真实全链路穿透」章节；
  构建状态/验证命令数字校准。
- **三线终态**：dotnet 0/0 · 139/139 · pytest **453**（436→453）零警告。未 commit。
