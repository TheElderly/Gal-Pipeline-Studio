# 核心数据模型与校验
pydantic>=2.7.0

# 本地形态素分词与词典 (0-Token 词频挖掘)
fugashi>=1.3.0
unidic-lite>=1.0.8

# 图分析与拓扑排序 (母子串血缘图)
networkx>=3.2.0

# 异步并发请求与流控
aiohttp>=3.9.0

# 图像处理与无损转码 (BMP 内存压榨)
Pillow>=10.3.0

# 单元测试与测试覆盖率
pytest>=8.0.0
pytest-asyncio>=0.23.0
pytest-cov>=5.0.0